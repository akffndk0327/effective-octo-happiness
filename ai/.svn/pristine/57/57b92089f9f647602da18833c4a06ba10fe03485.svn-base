package kr.or.ddit.vo;
/**
 * @author : 허민지
 * LomBok Annotation
 * @NoArgsConstructor : 기본 생성자
 * @AllArgsConstructor : 모든 파라미터 생성자
 * @RequiredArgsConstructor : final, @NonNull인 필드 값 파라미터.
 * (꼭 필요한 생성자 값들을 채워준다 무슨 말이냐 하면 필드 변수중에 final 필드 변수가 존재할 경우 생성자에서 자동으로 값을 넣어준다.)
 * @ToString : 필드에 대해서 toString 메소드 생성 자동으로 모든 필드 변수에 대해서 string 리턴 /exclude로 제외 가능
 * @EqualsAndHashCode : Equal, HashCode 만들기 /of= {"",""}로 한정되게 만들 수 있음.
 * (@EqualsAndHashCode(callSuper=false) 을 하면 자신 클래스만 허용 부모클래스x )
 * @Data : @Getter, @Setter, @RequiredArgsConstructor, @ToString, @EqualsAndHashCode 모두 포함된 것
 * 
 * HiberNate Validation
 * @NotBlank : 'String'이 null일 수 없으며, legnth가 0보다 커야할 때 사용
 * @NotEmpty : CharSequence, Collection, Map or Array의 객체가 null과 empty 값(size > 0)이 될 수 없다
 * @NotNull : CharSequence, Collection, Map or Array의 객체가 null일 수 없다. 그러나 empty는 가능하다.
 * @Min(0) : min값 설정
 * @Pattern(regexp="") : 정규식 설정
 *
 */
public class AALookAtMeVO {

}
